# SpinMe demo

SpinMe is created by **Background Gremlin Group — Creating Unique Tools for Unique Individuals**.

The product demo sequence is startup → normal spin → acceleration → fast spin → controlled slowdown → branded end card.

The demo media is maintained separately from the source release so the public repository remains source- and binary-release focused.
